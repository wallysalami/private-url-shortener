<% content_for :stylesheets do %>

<% end %>

<% content_for :content do %>
  <nav class="navbar navbar-toggleable navbar-toggleable-xl navbar-inverse bg-inverse">
    <a class="navbar-brand" href="/my-urls">URL Shortener</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="nav navbar-nav mx-auto justify-content-center">
      </ul>
      <ul class="navbar-nav"> 
        <li class='nav-item'>
          <a class="nav-link" href="/logout">
            <%=t :logout%>
          </a>
        </li>
      </ul>
    </div>
  </nav>
    
  <div id='main-container' class='d-flex justify-content-center'>
    <div id='center-container'>
      <%= content_for?(:shortened_url_content) ? yield(:shortened_url_content) : yield %>
    </div>
  </div>
<% end %>

<%= render template: "layouts/application" %>